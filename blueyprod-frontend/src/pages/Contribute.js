import React from "react";

// Minimal test component to verify CI build
export default function Contribute() {
  return (
    <div>
      <h1>Contribute (test build)</h1>
      <p>If you see this on Vercel, the build succeeded.</p>
    </div>
  );
}
